package fuzzy;

import fuzzy.Parser;
import fuzzy.Lexer;

class FuzzyEngine
{
  public function new()
  {
  }

  public function loadString(string : String)
  {
  }

  var parser : Parser;
  var lexer  : Lexer;
}